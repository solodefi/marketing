<head>
  <meta name="title" content="making new transaction page">
  <meta name="description" content="Making new transaction for architects, surveyors and structural engineers.">
  <meta name="keywords" content="trasaction, freelance architects, loft conversion">
</head>
<div class="container">
	</br>
		<h2 align="center">Send the payment to <%= @transaction.recipient.name %></h2>
	</br>
</div>

<div class = "container" id = "job_form">
  <div class="col-lg">
  <%= form_for(@transaction) do |f| %>
    <% if @transaction.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@transaction.errors.count, "error") %> prohibited this transaction from being saved:</h2>

        <ul>
        <% @transaction.errors.full_messages.each do |message| %>
          <li><%= @transaction %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="panel panel-default">
      <div class="panel-heading">

        <div class="form-group">
          <%= f.hidden_field :sender_id, autofocus: true, class: "form-control", placeholder: "Score", required: true %>
        </div>

        <div class="form-group">
          <%= f.hidden_field :recipient_id, autofocus: true, class: "form-control" , placeholder: "", required: true %>
        </div>

        <div class="form-group">
          <%= f.hidden_field :job_id, autofocus: true, class: "form-control" , placeholder: "", required: true %>
        </div>

        <div class="form-group">
          <%= f.hidden_field :status, autofocus: true, class: "form-control" , placeholder: "", required: true %>
        </div>

        <div class="form-group">
          <%= f.label :amount, "Amount (£)" %>
          <%= f.number_field :amount, autofocus: true, class: "form-control" , placeholder: "", required: true %>
        </div>
        
        <br>
        <div class="form-group" id = "button">
          <%= image_submit_tag "https://www.paypal.com/en_US/i/btn/btn_xpressCheckout.gif" %>
        </div>
      </div>
    </div>
  <% end %>
  </div>
</div>